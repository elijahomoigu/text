
// const calc = (x, y) =>{
//     x > y ? alert('') : alert('');
// }

// if(3 > 1){alert('correct!')}
/*
let x = 9;
if (x > 5 && x < 10) {
  console.log(true);
} else {
  console.log(false);
}


const friends = {
    name: "peter",
    age: "20",
    residence : "iraq",
}

logFunction = (x) =>{
    console.log(x)
}
// logFunction(friends)
class worker{
constructor (name, age, career){
    this.name = name;
    this.age = age;
    this.career = career;
    // this.details = console.log(this.name, this.age, this.career);
}
details(){
    console.log(`My name is ${this.name}, i am ${this.age} years old`)
}
}
const workerOne = new worker ('peter', 30, "dev ops");
workerOne.details()
// newOne = () =>{
//     console.log(workerOne.name, workerOne.age, workerOne.career);
//     // console.log(workerOne.age);
//     // console.log(workerOne.career);
// }
// newOne();

workerOne.details

*/

// let arr = [1, 2, 3, 4];
// let addUp = 0;

// for(let i = 0; i < arr.length; i++){
//     addUp += arr[i];
//     console.log(addUp);
// };
/*
class addArr {
  constructor(arr) {
    this.array = arr;
  }
  addUp() {
    let str = 0;
    for (let i = 0; i < this.array.length; i++) {
      str += this.array[i];
    }
    console.log(str);
  }
}

const newOne = new addArr([10, 20, 50]);
newOne.addUp();

class list {
  constructor(temp) {
    this.temp = temp;
  }
  toFarenheit() {
    const convert = (this.temp * 9) / 5 + 32;
    console.log(convert);
  }
}
const addition = new list(70);
addition.toFarenheit();

const newTwo = Math.trunc(Math.random() * 10);
// console.log(newTwo)
const arr = [2, 3, 4, 5];

const randTxt = Math.floor(Math.random() * arr.length);
console.log(randTxt);



const items = [254, 45, 212, 365, 2543];
console.log([Math.floor(Math.random()*items.length)]);


function random_item(items)
{
  b
return items[Math.floor(Math.random()*items.length)];
     
}

var items = [254, 45, 212, 365, 2543];
console.log(random_item(items));



class calcFunc {
    constructor(arr){
        this.arr = arr;
    }
    calc(){
        return items[Math.floor(Math.random()*items.length)];
    }
}

const Create = new calcFunc([30, 50, 200, 60])
console.log(Create.calc())
*/